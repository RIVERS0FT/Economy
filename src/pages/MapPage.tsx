import type { LoadedGameViewModel } from '../app/gameViewModel';

export function MapPage(_: { model: LoadedGameViewModel }) {
  return <section className="province-map-page" aria-label="美国本土州级经营地图页面" />;
}
