# Economy 市场行情图布局设计

> 上位文档：`PAGE_CONTENT_AND_NAVIGATION_DESIGN.md`、`UI_DESIGN_SYSTEM.md`

## 1. 规则优先级

本文件只负责市场页近 30 天按日行情图的绘图区、坐标、悬浮交互与安全区几何。与通用页面文档发生冲突时，本文件的行情图专项规则优先。

`PAGE_CONTENT_AND_NAVIGATION_DESIGN.md` 中“窄屏优先压缩成交量绘图区”“完整行情图固定使用 16:9”“移动端必须保留可见‘时间’横轴标题”的旧表述已被本文件废止。`16:9` 只允许作为桌面基础比例与首次渲染回退，不得作为窄屏最终强制比例。旧版“价格区与成交量区必须保持正间距”的规则同样废止，当前使用零间距连续双 Grid。

## 2. ECharts 唯一实现

- 市场行情图必须使用共享 `EconomyChart` 和 Apache ECharts 的 **ECharts SVG** 渲染，不得恢复手写 `<svg>`、`polyline`、`polygon` 或成交量 `rect`。
- 图表固定使用价格区和成交量区两个纵向 **双 Grid**；价格 `line` 与成交量 `bar` 共享同一最近 30 个北京时间自然日的日期尺度。
- 两个 Grid 必须零间距连续排列，并在共同边界绘制稳定 `1px` 分界线；不得重新使用空白间距把它们拆成两个独立图表。
- ECharts 只负责渲染。最近 30 天按日聚合、整数坐标、颜色方向、动态刻度和几何结果仍由项目纯函数计算，业务规则不得交给库默认值猜测。
- 业务页面不得直接调用 `echarts.init`；初始化、`ResizeObserver`、逐帧合并 resize、Option 更新和 `dispose()` 统一由 `EconomyChart` 负责。
- 市场组件必须在外层提供稳定 `data-*` 几何、刻度与交互接口；浏览器回归不得依赖 ECharts 私有 SVG 类名或内部 DOM 层级。
- 移动端触控高亮必须按通用触摸规则统一关闭：共享 `.economy-chart` 根节点、`.economy-chart__canvas`、ECharts 动态生成的 SVG／Canvas 子节点，以及直接绑定 Pointer Event 的 `.market-history-chart` 外层都必须计算为透明 `-webkit-tap-highlight-color`。实现唯一放在 `src/styles/mobile-interaction.css`，不得通过禁用 Pointer Event、Tooltip 或键盘 `:focus-visible` 规避蓝色区域。

## 3. 数据与动态坐标基线

- 行情固定统计当前资产最近 30 个北京时间自然日，每个自然日一个桶，共 30 个连续日桶；当天桶实时读取当日成交量和当前官方价，已完成日桶由服务器持久化，缺失自然日以零成交量并延续最近有效价格补齐。
- 价格折线位于上方，成交量柱状图位于下方；两个 x 轴必须使用同一动态时间间隔并保持网格线对齐。
- 横轴日期间隔必须根据真实绘图区宽度、根字号和 compact／full 变体，从 `1／2／3／5／7／10／15` 天中选择，宽屏显示更多日期刻度，窄屏或放大字号自动减少刻度。
- 价格与成交量纵轴的目标刻度数必须按各自真实像素高度和根字号动态计算；价格轴仍只能显示整数刻度，步长使用 `1／2／5 × 10^n`。
- 价格纵轴必须从所有合法整数候选中选择额外空白最少的区间，再以目标刻度数作为次级排序；不得为了凑固定刻度数把约 `3～6` 的真实数据扩展成 `0～10`。只有真实最低价接近零或完整覆盖数据确有需要时才能包含零点；恒定价格必须围绕当前整数价格保留对称或近似对称的最小可读区间。
- 价格折线固定使用信息色 `--color-info`，与净主动买入的成功色 `--color-success`、净主动卖出的危险色和方向未知的中性色保持不同视觉语义；价格面积填充必须复用信息色并降低不透明度，不得再用成功色冒充价格色。
- 零间距双 Grid 的共享边界只能显示一项纵轴刻度标签：价格轴保留最小刻度，成交量轴隐藏最大刻度。完整行情图的成交量轴目标刻度不得低于 3 个，因此隐藏最大刻度后仍必须至少显示 `0` 和一个非零中间刻度；compact 变体允许最低 2 个刻度。
- 成交量轴继续使用 K／M／B／T 紧凑格式；这是行情图自身的固定坐标规则，不依赖设置页开关。

## 4. 统一悬浮交互

- 价格与成交量不得分别处理悬浮。两个 x 轴必须通过 `axisPointer.link` 联动，鼠标位于任一 Grid 时都指向同一个 单日日桶。
- 竖向指针必须在零间距双 Grid 中保持同一 x 坐标、样式和虚线节奏，视觉上从价格区连续贯穿成交量区。
- 全图只显示一个 Tooltip；其数据索引必须由统一 `axisValue` 映射到分段，不得依赖当前触发 Series 的 `dataIndex`。
- Tooltip 只展示当前日桶的日期、价格、总成交量、主动买入、主动卖出、方向未知和净主动量，不得显示玩家身份、订单来源或交易对手。
- 指针首次进入行情数据区或在数据区横向移动时，行情图外层交互必须按当前横向位置主动驱动同一分段的 Tooltip 与联动 AxisPointer；不得依赖 ECharts SVG／ZRender 子节点恰好收到原生 `mousemove`。第一次有效 `pointermove` 即必须显示当前分段 Tooltip，并通过 `requestAnimationFrame` 合并连续高频移动。
- AxisPointer 必须关闭自动 Series emphasis，价格线和成交量柱也必须禁用 hover emphasis；悬浮前后价格折线的颜色、宽度与不透明度必须保持不变，不得隐藏或被面积层覆盖。
- 价格折线必须保持高于自身面积填充的稳定渲染层级。
- 当指针仍位于行情数据区时，普通 `5s` 状态轮询、无关 React 重渲染和尺寸未变化的页面更新不得清除当前 Tooltip 或 AxisPointer。
- 真实行情数据或 6 分钟窗口变化需要更新 ECharts Option 时，必须在 Option 应用后恢复鼠标当前横向位置对应的分段 Tooltip；不得要求用户再次移动鼠标。
- Tooltip 只允许在指针离开行情数据区、资产切换、页面卸载或用户主动关闭时隐藏。离开数据区时必须同时取消尚未执行的主动显示帧，防止隐藏后旧帧再次闪回；不得使用 `alwaysShowContent`、超长 `hideDelay` 或禁止正常离开隐藏来掩盖更新重置问题。

## 5. 成交量绘图区可读性

完整行情图的成交量绘图区必须保持最低可读屏幕高度，不得作为底部标注空间不足时的压缩对象。

- full 变体任意支持断点下成交量绘图区实际屏幕高度不得低于 `68px`，并随根字号放大；compact 变体最低仍为 `48px`。
- 成交量绘图区不得低于价格区与成交量区合计数据绘图区的 `22%`；零间距分界线不计入数据区高度。
- full 变体成交量纵轴至少生成 3 个刻度，隐藏共享边界最大刻度后仍须保留一个非零可见刻度；刻度不得互相覆盖。
- 所有非零成交柱必须设置最小可见高度 `2px`，同时继续使用线性纵轴和真实数值比例；不得通过对数轴、平方根轴或修改成交量数据放大小成交。
- 成交量柱必须完整位于成交量绘图区内。
- 价格区底边与成交量区顶边必须使用同一坐标；视觉分区仅由 `1px` 分界线完成。
- 共同边界的标签所有权固定属于价格轴。成交量最大值仍可通过 Tooltip 和其余动态刻度判断，不得在边界重复显示。

成交量高度必须取以下三项的最大值：桌面首选高度、满足 full／compact 最低高度和字号间距所需高度、满足 `22%` 数据区占比所需高度。

## 6. 动态总高度与轴标题



当前 grand-strategy 玩家市场页的正式布局保持一列紧凑市场卡，不以恢复“生产页面宽屏自动双列”作为要求。地区商品详情的成交趋势图必须由 `.market-chart-card.ui-entity-card` 承载，复用现有实体卡的圆角、边界、实体背景和共享 inset；卡内不显示商品名、趋势标题或涨跌标签，不得为图表另建卡片视觉或使用毛玻璃。详情接口不可用时，图表组件不渲染，卡内状态容器改为带 `aria-disabled` 的居中禁用状态，保留图表相近高度；不得显示原始接口报错。`PriceSparkline` 仍必须保留宿主**明确进入双列**时的 row-fill 能力：在宿主显式进入双列时，交易卡和行情卡外框必须同排等高，行情组件以行情卡内容盒底边为基准读取标题下方的真实剩余高度作为最小总高度，计算时排除卡片边框和底部内边距。该能力用于组件级回归、嵌入式宿主或未来显式双列场景，不得反向要求当前战略市场页恢复双列。



桌面端纵轴标题继续垂直显示。容器宽度不大于 `720px` 时，ECharts 纵轴名称必须关闭，并在价格区和成交量区左上角分别渲染水平小标题；标题使用共享表面与文本令牌，不得挤占纵轴数字标签。左侧 gutter 必须根据价格与成交量实际格式化后的最长可见标签估算宽度，移动端不得继续固定保留桌面纵向标题的宽度。

## 7. 底部安全区


- 时间刻度不得侵入成交量绘图区；

安全区必须根据实际屏幕字号和最长 `HH:mm` 标签旋转后的真实占用动态计算。移动端取消可见“时间”标题后，释放的高度必须真实减少组件总高度，不能保留空占位。

### 7.1 市场数据可视化配套规则

`PAGE_CONTENT_AND_NAVIGATION_DESIGN.md` 继续负责全局市场、地区市场和商品详情的页面职责与州级经济隔离；本节锁定与行情图加载和地区商品详情紧凑信息层级直接相关的规则。

- 全局市场默认页不显示顶部统计摘要条或独立地区市场面板；商品标题、默认折叠筛选和商品目录直接作为 `GlobalMarketPage` 正文内容。点击商品后进入同页商品全局详情，再从地区行情行进入既有地区商品详情；商品全局详情同样不显示“地区行情”标题、地区计数或外层一级卡片，筛选、共享表头和地区商品列表直接作为正文内容。
- 全局市场商品行、筛选和商品全局详情地区行的响应式收敛必须依据 `GlobalMarketPage` 正文承载宽度而不是浏览器 viewport；列表轨道允许 `minmax(0, 1fr)` 收缩，不得制造横向主滚动。
- 全局市场商品目录不再承载跨州覆盖条。商品全局详情的地区行情行只显示地区、今日价格、24h 成交量和 24h 价格变化；不得把不同地区订单簿、库存或价格合并为全国数值。
- 地区商品详情最上方使用四项交易摘要：今日价格、今日成交量、可用库存和冻结库存。摘要字段与模块归属由 `PAGE_CONTENT_AND_NAVIGATION_DESIGN.md` 定义；本 DESIGN 仅锁定其与行情图的相邻布局。相对基础价偏离、需求满足率、需求参考价／参考价和上轮需求不属于详情可见信息。
- 地区商品详情不渲染商品 hero 指标条、基本面条或商品基本面卡。四项顶部摘要按市场正文真实容器宽度响应：宽容器保持四列，不大于 `720px` 时收敛为两列；不得保留旧字段空轨道或造成横向溢出。
- 页面顺序固定为“顶部交易摘要 → 近 30 天按日行情图 → 手动即时交易 → 浏览器本地成交记录”。行情图、手动交易区和本地成交直接排列在页面内容区；手动交易区不使用一级卡片底座，但内部操作边界继续保留。地区商品详情不得渲染自动经营执行卡或玩家订单簿。
- 成交趋势详情接口提供最近 30 个北京时间自然日的 `dailyHistory`；已完成商品日桶由服务器持久化，当前日桶实时合并 `todayBuyQuantity`／`todaySellQuantity` 与当前官方价。原始 `priceHistory` 继续只承担兼容与 24h 摘要用途，不再决定 30 天图表的保留能力。客户端详情刷新跟随所选市场公开摘要的原始数值变化，不依赖 `game.orders` 或市场对象引用变化；已有有效详情时，单次后台刷新失败不得覆盖当前行情或已经确认的交易结果。
- 中窄与移动布局的共享商品数据行仍必须保留商品、24h 成交量、今日价格和 24h 价格变化，并保持单行；不得恢复盘口、挂单差额、基准偏离或挂单状态等字段，也不得通过横向滚动维持桌面列宽。

## 8. 浏览器回归

必须在以下尺寸验证真实几何：

- `1684 × 931` 桌面端；
- `1440 × 900` 桌面浏览器中的 building 窄市场正文，验证真实内容容器不大于 `720px` 时的详情响应；
- `721 × 445` 共享边界刻度问题复现场景；
- `390 × 844` 移动端；
- `320 × 700` 极窄移动端；
- `390 × 844` 且根字号放大到 `125%`。


`tests/browser/market-detail-direct-flow.spec.ts` 必须在 `1440 × 900` 的真实 building 市场正文中验证：顶部摘要固定为“今日价格／今日成交量／可用库存／冻结库存”，旧 hero 指标条、基本面条和自动经营执行卡不存在，手动交易区没有一级卡片底座，并且行情卡在正常区块间距内紧随摘要；`390 × 844` 继续验证同一结构不造成横向溢出且四项摘要完整可见。

`tests/browser/market-chart-height-stability.spec.ts` 必须在 `1684 × 931` 中显式把**测试宿主**和 `.unified-market-grid` 切换为双列，再连续采样至少 `120` 个动画帧并跨越正式 `5s` 轮询周期等待至少 `6.5s`，确认行情图、行情卡和最小高度的波动均不超过 `1px`。测试不得只放宽外层 `workspaceCard` 后假定生产 CSS 会自动进入双列。随后删除测试双列覆盖并切换到 `1280 × 900`，确认填充模式恢复为自然高度且继续稳定。

共享边界回归必须读取实际 ECharts SVG 文字节点，确认价格最小刻度存在、成交量最大刻度不存在，并且价格轴左侧的共同边界区域只有一项可见纵轴刻度。只检查 Option 字符串或 `data-*` 声明不足以证明问题已修复。

`tests/browser/market-chart-readability.spec.ts` 必须在 `390 × 844` 实际页面中验证：约 `10～12` 的价格数据不会包含零点或扩展到无关大区间；成交量刻度数不少于 3 且存在非零中间刻度；价格色角色为 info 且与 success 色不同；移动水平分区标题存在、左侧 gutter 小于桌面旧固定值、可见“时间”标题不存在，并读取实际 ECharts SVG 非零成交量标签。

浏览器交互回归还必须分别在价格区和成交量区移动到同一横坐标，确认 Tooltip 内容一致、联动指针启用、Series emphasis 已禁用且页面无运行时错误；并必须使用只到达行情图外层的合成 `pointermove` 证明第一次有效指针移动即可主动显示 Tooltip，不能仅依赖 ECharts 子节点的原生鼠标事件。

Tooltip 持久性回归必须让父组件至少每秒发生一次无关重渲染，鼠标静止超过正式 `5s` 轮询周期并等待至少 `6.5s`，随后再触发一次真实行情 Option 更新；两阶段 Tooltip 均须保持可见、ECharts 实例 ID 不变，真实更新后内容须刷新。最后必须验证指针离开图表时 Tooltip 正常隐藏。

`tests/browser/mobile-chart-tap-highlight.spec.ts` 必须在 `390 × 844`、`hasTouch: true` 和 `isMobile: true` 的 Chromium 上下文中，读取行情图外层、共享图表根、画布宿主、实际 SVG 与路径节点的计算样式，确认 `-webkit-tap-highlight-color` 全部透明，并执行一次真实 `tap()`；只检查 CSS 源码字符串不足以证明动态 ECharts 子节点已被覆盖。

## 9. 防回退

不得：

- 恢复“窄屏优先压缩成交量绘图区”；
- 让 full 成交量图区低于 `68px`、compact 低于 `48px`，或低于数据绘图区的 `22%`；
- 让 full 成交量轴退回两个刻度并在隐藏最大刻度后只剩 `0`，或删除非零柱 `2px` 最小可见高度；
- 恢复价格区与成交量区之间的空白间距，或删除共同边界的 `1px` 分界线；
- 在共享边界同时显示价格最小刻度和成交量最大刻度，或把标签所有权改给成交量轴；
- 为凑固定刻度数无理由把窄幅价格数据扩展到零点或远离真实数据的范围；
- 把上下 Grid 恢复为分别触发 Tooltip、分别选取分段或不同步的 AxisPointer；
- 让首次有效指针移动重新依赖 ECharts 子节点原生 `mousemove` 才显示 Tooltip，或删除外层主动分段驱动与逐帧合并；
- 让 AxisPointer 或 Series emphasis 改变、模糊或隐藏价格折线；
- 让普通轮询、无关重渲染或真实行情 Option 更新在鼠标静止时清除 Tooltip，或用 `alwaysShowContent`／超长 `hideDelay` 规避正常交互生命周期；
- 恢复固定 12 个两小时横轴分段、固定 3／5 个价格刻度或固定 2／3 个成交量刻度；
- 用固定 `16:9` 最终覆盖动态比例，或把当前战略市场页改回自动宽屏双列来满足 row-fill 测试；显式双列宿主中仍不得让行情卡与交易卡不同高；
- 使用行情卡 border-box 底边或监听图表自身高度反推最小高度，形成每轮增长的尺寸反馈；
- 在移动端继续使用桌面纵向轴标题和固定大左边距，或让水平分区标题覆盖刻度和数据；
- 让成交量刻度重叠、柱越出成交量区或侵入价格区；
- 只覆盖按钮或链接的 tap highlight，而遗漏 `.economy-chart`、`.economy-chart__canvas` 的动态子节点或 `.market-history-chart` 指针交互外层；
- 绕过 `EconomyChart` 直接初始化 ECharts，或用 ECharts 私有 DOM 作为测试合同；
- 在地区商品详情恢复市场价、基准偏离、需求满足率、参考价、上轮需求、基本面条、自动经营执行卡或手动交易一级卡片底座；
- 让详情接口重新发送全部历史或合成行情点，或让客户端因 `game.orders`／对象引用变化重复请求同一详情。

本文是市场行情图几何、交互与可读性唯一专项基线；数据生成和撮合价格仍归市场业务设计。

- 防回退：成交趋势必须保持最近 30 个北京时间自然日、按日 30 桶；不得恢复 24h／6 分钟 240 桶，也不得恢复“净主动买入／净主动卖出”可见图例。
