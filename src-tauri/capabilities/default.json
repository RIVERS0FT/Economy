{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Economy Arena.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
