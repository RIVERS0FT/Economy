import { replaceExact } from './helpers.mjs';

replaceExact("server/src/market-demand.js", "import { ordersForDemandGroup } from './order-book-runtime.js';", "import { ordersForDemandGroup, recordOrderBookReduction } from './order-book-runtime.js';");
replaceExact("server/src/market-demand.js", "    order.quantity = filledQuantity + keep;\n    order.remaining = keep;\n    order.status = keep <= 0 ? (filledQuantity > 0 ? 'filled' : 'cancelled') : (filledQuantity > 0 ? 'partial' : 'open');\n    return keep;", "    order.quantity = filledQuantity + keep;\n    order.remaining = keep;\n    order.status = keep <= 0 ? (filledQuantity > 0 ? 'filled' : 'cancelled') : (filledQuantity > 0 ? 'partial' : 'open');\n    recordOrderBookReduction(world, order, removed);\n    return keep;");
replaceExact("server/src/market-demand/state.js", "import { ensurePopulationEconomy, releasePopulationOrderFunds } from '../population-economy.js';", "import { ensurePopulationEconomy, releasePopulationOrderFunds } from '../population-economy.js';\nimport { closeOrderInOrderBook } from '../order-book-runtime.js';");
replaceExact("server/src/market-demand/state.js", "  function normalizePlayerActivity(world, now) {\n    world.players ||= {};\n    const latestOrderAt = new Map();\n    for (const order of world.orders || []) {\n      if (order.ownerType !== 'player' || !Number.isFinite(Number(order.ownerId))) continue;\n      const ownerId = String(order.ownerId);\n      latestOrderAt.set(ownerId, Math.max(latestOrderAt.get(ownerId) || 0, Number(order.createdAt || 0)));\n    }\n    for (const [playerId, player] of Object.entries(world.players)) {\n      if (Number.isFinite(Number(player.lastEconomicActivityAt)) && Number(player.lastEconomicActivityAt) > 0) continue;\n      const tradeAt = (player.trades || []).reduce((latest, item) => Math.max(latest, Number(item.createdAt || 0)), 0);\n      const ledgerAt = (player.ledger || []).reduce((latest, item) => Math.max(latest, Number(item.createdAt || 0)), 0);\n      const inferred = Math.max(\n        Number(player.registeredAt || 0),\n        Number(player.work?.lastWorkedAt || 0),\n        latestOrderAt.get(playerId) || 0,\n        tradeAt,\n        ledgerAt,\n      );\n      player.lastEconomicActivityAt = inferred > 0 ? inferred : now;\n    }\n  }", "  function normalizePlayerActivity(world, now) {\n    world.players ||= {};\n    const missingPlayers = Object.entries(world.players).filter(([, player]) => !(\n      Number.isFinite(Number(player.lastEconomicActivityAt)) && Number(player.lastEconomicActivityAt) > 0\n    ));\n    if (missingPlayers.length === 0) return;\n\n    const missingPlayerIds = new Set(missingPlayers.map(([playerId]) => playerId));\n    const latestOrderAt = new Map();\n    for (const order of world.orders || []) {\n      if (order.ownerType !== 'player' || !Number.isFinite(Number(order.ownerId))) continue;\n      const ownerId = String(order.ownerId);\n      if (!missingPlayerIds.has(ownerId)) continue;\n      latestOrderAt.set(ownerId, Math.max(latestOrderAt.get(ownerId) || 0, Number(order.createdAt || 0)));\n    }\n    for (const [playerId, player] of missingPlayers) {\n      const tradeAt = (player.trades || []).reduce((latest, item) => Math.max(latest, Number(item.createdAt || 0)), 0);\n      const ledgerAt = (player.ledger || []).reduce((latest, item) => Math.max(latest, Number(item.createdAt || 0)), 0);\n      const inferred = Math.max(\n        Number(player.registeredAt || 0),\n        Number(player.work?.lastWorkedAt || 0),\n        latestOrderAt.get(playerId) || 0,\n        tradeAt,\n        ledgerAt,\n      );\n      player.lastEconomicActivityAt = inferred > 0 ? inferred : now;\n    }\n  }");
replaceExact("server/src/market-demand/state.js", "        releasePopulationOrderFunds(world, order);\n        order.status = 'cancelled';", "        releasePopulationOrderFunds(world, order);\n        order.status = 'cancelled';\n        closeOrderInOrderBook(world, order);");
replaceExact("server/src/market-liquidity.js", "import { bestSystemOrder as indexedBestSystemOrder, ordersForDemandGroup } from './order-book-runtime.js';", "import {\n  bestSystemOrder as indexedBestSystemOrder,\n  ordersForDemandGroup,\n  recordOrderBookReduction,\n} from './order-book-runtime.js';");
replaceExact("server/src/market-liquidity.js", "  function releaseOpenOrder(world, order) {\n    if (!isOpenOrder(order)) return;\n    const groupState = groupStateFor(world, order.demandGroupId);\n    const reserve = reserveFor(world, order.demandGroupId, order.productId);\n    if (!groupState || !reserve) {\n      order.status = 'cancelled';\n      order.remaining = 0;\n      return;\n    }\n    const remaining = Math.max(0, Math.floor(Number(order.remaining || 0)));\n    if (order.demandTier === LIQUIDITY_BUY) {\n      const release = multiplyMoneyByInteger(order.price, remaining) || 0;\n      groupState.frozenCredits = roundMoney(groupState.frozenCredits - release);\n      groupState.credits = roundMoney(groupState.credits + release);\n    } else if (order.demandTier === LIQUIDITY_SELL) {\n      reserve.frozenInventory = Math.max(0, reserve.frozenInventory - remaining);\n      reserve.inventory += remaining;\n    }\n    order.remaining = 0;\n    order.status = 'cancelled';\n  }", "  function releaseOpenOrder(world, order) {\n    if (!isOpenOrder(order)) return;\n    const remaining = Math.max(0, Math.floor(Number(order.remaining || 0)));\n    const groupState = groupStateFor(world, order.demandGroupId);\n    const reserve = reserveFor(world, order.demandGroupId, order.productId);\n    if (!groupState || !reserve) {\n      order.status = 'cancelled';\n      order.remaining = 0;\n      recordOrderBookReduction(world, order, remaining);\n      return;\n    }\n    if (order.demandTier === LIQUIDITY_BUY) {\n      const release = multiplyMoneyByInteger(order.price, remaining) || 0;\n      groupState.frozenCredits = roundMoney(groupState.frozenCredits - release);\n      groupState.credits = roundMoney(groupState.credits + release);\n    } else if (order.demandTier === LIQUIDITY_SELL) {\n      reserve.frozenInventory = Math.max(0, reserve.frozenInventory - remaining);\n      reserve.inventory += remaining;\n    }\n    order.remaining = 0;\n    order.status = 'cancelled';\n    recordOrderBookReduction(world, order, remaining);\n  }");
