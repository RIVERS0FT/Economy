import { appendOnce, replaceExact } from './helpers.mjs';

replaceExact(
  'docs/UNIFIED_ASSET_ORDER_BOOK_DESIGN.md',
  '商品订单和工厂订单不得互相撮合；不同商品或不同工厂类型也不得互相撮合。不同主体之间可成交的交叉订单必须在新订单进入订单簿时立即按 maker price 撮合，直到新订单完成、对手盘耗尽或最高买价严格低于最低卖价；不得先公开展示可成交交叉盘口再等待下一次轮询处理。`world.orders` 继续是唯一持久化权威订单集合；`order-book-runtime.js` 只为当前工作世界按资产和方向建立价格时间优先索引，同价且时间相同时必须保持原数组顺序。索引识别尾部追加并对数组整体替换自动重建，订单成交、撤销和部分成交直接读取同一订单对象的当前状态；索引不得进入世界 JSON、客户端分区、分区哈希、估值或 SQLite。撮合、自交叉、系统最优价、玩家未完成订单数量、商品买单预占、工厂卖单冻结和人口卖盘深度不得各自重新对完整 `world.orders` 过滤排序。',
  '商品订单和工厂订单不得互相撮合；不同商品或不同工厂类型也不得互相撮合。不同主体之间可成交的交叉订单必须在新订单进入订单簿时立即按 maker price 撮合，直到新订单完成、对手盘耗尽或最高买价严格低于最低卖价；不得先公开展示可成交交叉盘口再等待下一次轮询处理。`world.orders` 继续是唯一持久化权威订单集合；`order-book-runtime.js` 只为当前工作世界按资产和方向建立价格时间优先索引，同价且时间相同时必须保持原数组顺序。索引识别尾部追加并对数组整体替换自动重建，订单成交、撤销和部分成交直接读取同一订单对象的当前状态；索引不得进入世界 JSON、客户端分区、分区哈希、估值或 SQLite。撮合、自交叉、系统最优价、玩家未完成订单数量、商品买单预占、工厂卖单冻结和人口卖盘深度不得各自重新对完整 `world.orders` 过滤排序。\n\n运行时索引必须保持一个按资产与方向组织的统一混合盘口，玩家订单、消费需求订单和市场储备订单不得拆成独立玩家盘口或系统盘口。全量建立索引时必须先单次遍历未完成订单完成分组，再对每个既有分组统一排序；不得在全量构建循环中逐单对有序数组执行中部 `splice`。已成交或已撤销订单只保留在 `world.orders` 历史中，不进入活动盘口、玩家开放订单集合或需求组开放订单集合。尾部新订单仍可二分插入当前分组；成交缩量与撤单必须同步更新运行时开放订单计数、商品买单预占和工厂卖单冻结聚合。当前版本迁移、无变化历史剪枝和不存在旧系统工厂订单的清理必须保持原 `world.orders` 数组引用；真实剪枝只淘汰过期或超过 800 笔的已关闭历史，不得因历史保存上限删除任何未完成订单。',
);

appendOnce('docs/README.md', '61. 统一订单簿运行时性能', '\n61. 统一订单簿运行时性能属于订单簿与服务器共同规则：`world.orders` 保持唯一持久化权威集合和单一混合盘口，不得按玩家／系统拆分盘口；全量索引必须先分组后排序且只索引未完成订单，尾部追加继续增量插入，成交缩量与撤单同步维护开放订单、商品买单预占和工厂卖单冻结聚合。当前版本迁移、无变化剪枝和无旧系统工厂订单清理必须保持订单数组引用；历史剪枝只限制已关闭订单为最近 800 笔，绝不得删除未完成订单。必须同步更新 `UNIFIED_ASSET_ORDER_BOOK_DESIGN.md`、`SERVER_ARCHITECTURE_AND_DEPLOYMENT_DESIGN.md`、服务器测试、`scripts/verify-order-matching-core.mjs` 与 `scripts/verify-runtime-efficiency.mjs`。\n');

appendOnce('docs/SERVER_ARCHITECTURE_AND_DEPLOYMENT_DESIGN.md', '## 统一订单簿运行时容量边界', '\n\n## 统一订单簿运行时容量边界\n\n`server/src/order-book-runtime.js` 是 `world.orders` 的事务内唯一派生索引。索引继续使用按资产与方向组织的单一混合盘口，不得按玩家订单与系统订单拆分第二套盘口。全量构建只允许一次遍历未完成订单完成分组，再对各分组统一排序；已关闭订单不进入活动盘口。尾部追加、成交缩量、撤单和系统订单释放必须同步更新玩家开放订单数、商品买单仓库预占与工厂卖单冻结聚合。\n\n当前版本迁移、无变化订单历史维护和不存在旧系统工厂订单时不得替换 `world.orders` 引用；只有真实迁移删除或历史剪枝才允许整体替换并触发索引重建。订单历史最多保留最近 800 笔未过期关闭订单，全部未完成订单必须无条件保留。订单 ID 查询与撤单必须复用 `orderById`，不得恢复全量线性查找。以上边界由订单簿运行时测试、剪枝测试、`scripts/verify-order-matching-core.mjs` 与 `scripts/verify-runtime-efficiency.mjs` 防回退。\n');
