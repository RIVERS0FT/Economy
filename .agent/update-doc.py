from pathlib import Path

path = Path('docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md')
text = path.read_text(encoding='utf-8')
replacements = [
    (
        '页面主标题固定为“合同”，使用路由 ID `contracts` 和 `ContractPage`。页面内部标签固定为“进行中的合同／合同广场／待处理／合同历史”，默认进入“进行中的合同”；页面右上角提供“发布合同”。合同页面的核心不是一次性交换，而是玩家之间的长期周期供货合作。',
        '页面主标题固定为“合同”，使用路由 ID `contracts` 和 `ContractPage`。页面右上角提供“发布合同”。桌面合同主体固定为四列工作区：左侧两列是常驻“合同广场”，右侧两列是“我的合同”；右侧只使用“进行中的合同／历史合同”两个共享分段按钮切换，默认进入“进行中的合同”。切换右侧内容不得卸载、隐藏或重排左侧合同广场。合同页面的核心不是一次性交换，而是玩家之间的长期周期供货合作。',
    ),
    (
        '合同页顶部只显示进行中的合同、等待我处理、24 小时内交付和我的公开合同四项摘要。合同页的四项摘要在宽布局四列同排，宽度不大于 `960px` 时固定为两列；移动端继续保持两列，不能退回四个单列摘要把合同主体推离首屏。进行中合同按宽限期、需要处理、即将交付和正常履约排序；卡片必须显示我采购或我供货、合作方、每批商品、单位价格、每批货款、交付周期、已完成批次、下次交付、供应方托管商品和采购方托管货款。商品不足、货款不足、采购方仓库不足、宽限期和批次后结束申请必须直接显示文字，颜色不得作为唯一状态表达。',
        '合同页顶部只显示进行中的合同、等待我处理、24 小时内交付和我的公开合同四项摘要。合同页的四项摘要在宽布局四列同排，宽度不大于 `960px` 时固定为两列；移动端继续保持两列，不能退回四个单列摘要把合同主体推离首屏。待处理判定统一包含宽限期、当前批次异常、批次后结束申请和等待本人确认的续签提议；进行中合同固定按宽限期、待处理、即将交付和正常履约排序。待处理卡片使用警示色边框与柔和背景，并同时显示“待处理”文字标签；宽限期继续使用更高优先级的危险色，颜色不得作为唯一状态表达。卡片必须显示我采购或我供货、合作方、每批商品、单位价格、每批货款、交付周期、已完成批次、下次交付、供应方托管商品和采购方托管货款。',
    ),
    (
        '标签栏必须复用 `Button` 与 `.ui-segmented`，桌面四项等宽，移动端保持完整中文名称并横向滚动；必须提供 `tablist`、`tab`、`tabpanel`、`aria-selected`、`aria-controls` 和 `aria-labelledby` 语义，不得重新实现页面专属基础按钮视觉。',
        '“我的合同”切换栏必须复用 `Button` 与 `.ui-segmented`，两项等宽并保持完整中文名称；必须提供 `tablist`、`tab`、`tabpanel`、`aria-selected`、`aria-controls` 和 `aria-labelledby` 语义，不得重新实现页面专属基础按钮视觉。待处理不再是独立标签，而是在“进行中的合同”数量旁显示警示计数并在列表中置顶。',
    ),
    (
        '移动端标签栏必须使用单行固有宽度 Flex 横向滚动；每个标签按钮覆盖共享分段按钮的 `width: 100%` 为 `width: auto`，并使用 `flex: 0 0 auto` 保持独立命中区域。选中背景、文字和数量徽标不得越出自身按钮或覆盖相邻标签，`320px` 与 `390px` 宽度下必须逐项可达且页面本身不得横向溢出。',
        '桌面视口不小于 `1400px` 时，合同广场和进行中合同各自使用两列自然高度卡片，合计每行最多四张合同卡；`961px–1399px` 时左右区域仍并排但各自单列；宽度不大于 `960px` 时两区上下堆叠并各自保持两列，宽度不大于 `720px` 时全部改为单列。页面只使用唯一纵向滚动区域，不得给左右区域增加独立滚动条。移动端两个切换按钮保持至少 `48px` 命中高度，选中背景、文字和数量徽标不得越出自身按钮或覆盖相邻按钮，`320px` 与 `390px` 宽度下页面不得横向溢出。',
    ),
    (
        '合同广场在宽度不小于 `1220px` 时使用双列自然高度卡片，较窄空间改为单列；公开合同卡按“方向与状态／商品和单位价格／四项条款／风险说明／承接或取消操作”组织，不得强制同行等高制造空白。合同历史使用单张一级 `PagePanel` 和带分隔线的紧凑记录行，移动端每行上下堆叠；不得恢复每条历史记录一张独立玻璃卡片。历史列表由独立 SQLite 合同审计接口按最终状态、我的角色、商品和日期范围筛选，并使用游标分页；记录行显示最终状态、角色、完成批次、累计货款、结束时间和“完整审计／旧数据摘要”标记。点击记录只在同一 `PagePanel` 内展开条款、累计货款／服务费／净收入／保证金赔付和追加式事件时间线，不创建页面级弹窗或第二套详情路由。',
        '公开合同卡按“方向与状态／商品和单位价格／四项条款／风险说明／承接或取消操作”组织，不得强制同行等高制造空白。四列工作区中的合同卡必须把标题、进度、当前批次、合同条款、自动操作和底部按钮按卡片可用宽度自然堆叠，不得通过缩小文字或隐藏条款强行容纳。合同历史使用右侧区域内的单张一级 `PagePanel` 和带分隔线的紧凑记录行；筛选器在右侧半宽区域使用两列，移动端改为单列，记录行在右侧区域内上下堆叠；不得恢复每条历史记录一张独立玻璃卡片。历史列表由独立 SQLite 合同审计接口按最终状态、我的角色、商品和日期范围筛选，并使用游标分页；记录行显示最终状态、角色、完成批次、累计货款、结束时间和“完整审计／旧数据摘要”标记。点击记录只在同一 `PagePanel` 内展开条款、累计货款／服务费／净收入／保证金赔付和追加式事件时间线，不创建页面级弹窗或第二套详情路由。',
    ),
    (
        '合同摘要网格、可选发布面板、标签栏和当前标签内容必须按该顺序作为 `PageLayout` 自动生成的 `.ui-page-stack` 直接子元素；相邻一级区块统一跟随 `--page-section-gap`，`contracts.css` 不得用外边距补齐或取消该间隔。该规则的结构静态检查和桌面／平板／移动真实几何回归必须随合同布局验证一起运行。',
        '合同摘要网格、可选发布面板和四列合同工作区必须按该顺序作为 `PageLayout` 自动生成的 `.ui-page-stack` 直接子元素；工作区内部左右区域使用 `--page-section-gap`，各区标题与卡片网格使用设计令牌间距，`contracts.css` 不得用外边距补齐或取消一级区块间隔。该规则的结构静态检查和桌面／平板／移动真实几何回归必须随合同布局验证一起运行。',
    ),
]

for old, new in replacements:
    if text.count(old) != 1:
        raise SystemExit(f'Expected exactly one design rule to replace: {old[:40]}')
    text = text.replace(old, new)
path.write_text(text, encoding='utf-8')
