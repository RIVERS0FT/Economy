import { existsSync, readFileSync, writeFileSync, unlinkSync } from 'node:fs';

const path = 'docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md';
let source = readFileSync(path, 'utf8');
const replacements = [
  [
    '未开放写权限的地区市场仍允许只读查看商品目录、今日官方价格和真实成交行情；只读态隐藏即时交易提交能力，但不得伪造空盘口或恢复撤单入口。自动经营策略与执行解释唯一归地区工厂详情；地区商品详情不维护第二套自动经营配置。',
    '连续 48 州均具有商品市场写权限；不存在基于地区解锁状态的只读市场分支。玩家仍需满足资金、库存及其他正式业务资格，自动经营策略与执行解释唯一归地区工厂详情；地区商品详情不维护第二套自动经营配置。',
  ],
  [
    '必须显示工厂类型、可选或固定产物、建造数量、建造现金、逐项建造材料需要量／库存／缺口和“库存可直接建”。材料不足时只读取按当前表单上下文取得的服务器只读报价，不得从主状态本人订单推测公共深度：全部缺料可购齐且不存在自己的交叉卖单时显示预计采购、预计总支出并允许“一键购齐并建造”；卖盘不足时显示“卖盘不足 · 可挂买单”、逐材料价格、买单最高占用和“一键提交缺料买单”，提交缺料买单时不得因自己的交叉卖单直接禁用按钮：服务器必须在同一幂等事务内自动撤销与各材料提交买价交叉的本人未完成商品卖单，释放其剩余冻结库存后重新计算真实缺口；客户端的买单最高占用与未完成订单配额提示也按这些预计撤单释放后的缺口和订单槽位计算。未交叉卖单保持不动，再按撤单后的缺口校验未完成订单配额以及“建造现金 + 买单最大冻结额”的可用资金。材料已经齐全时直接“立即建造”。建设卡下方的“待采购”按每次提交建造的工厂类型和数量独立聚类，显示该次提交关联的正式商品买单、已成交／目标／剩余与提交价格，并提供“取消全部”；取消只撤销该组剩余 `open`／`partial` 买单，已成交材料保留。采购组全部订单成交或取消后从建设卡本地聚类中移除；不得把不同提交合并、不得自动建厂、不得冻结建造现金，也不得在市场页合并底层订单或删除逐单撤销入口。',
    '必须显示工厂类型、可选或固定产物、建造数量、建造现金、逐项建造材料需要量／库存／缺口和“库存可直接建”。材料不足时只读取当前表单地区、工厂类型和数量对应的服务器只读报价；报价按各缺失材料当日 `officialPrice` 显示预计采购与预计总支出，不读取玩家盘口深度、本人订单或最优买卖价。报价就绪且资金充足时只提供“一键购齐并建造”：提交 `autoProcure=true`、逐材料价格保护和采购总额保护，服务器在同一幂等事务中重新读取当日官方价、即时购齐全部缺失材料并继续建厂；任一价格保护、资金、材料或建设校验失败时整笔事务回滚。材料已经齐全时直接“立即建造”。建设卡不得显示自定义建材买价、卖盘不足、买单最高占用、缺料买单、待采购组或取消采购入口，也不得创建 FOK、`open`／`partial` 商品订单或冻结等待成交资金。',
  ],
];
for (const [from, to] of replacements) {
  if (!source.includes(from)) throw new Error(`找不到页面设计待替换段落: ${from.slice(0, 50)}`);
  source = source.replace(from, to);
}
writeFileSync(path, source.endsWith('\n') ? source : `${source}\n`);
for (const temp of ['scripts/codex-clean-page-design-instant-market.mjs', '.github/workflows/codex-clean-page-design-instant-market.yml']) {
  if (existsSync(temp)) unlinkSync(temp);
}
