from pathlib import Path
import re

p = Path('docs/COMMERCIAL_BUILDINGS_DESIGN.md')
s = p.read_text().replace('服务器按参与数量一次性执行', '服务器按本周期锁定的整数等效经营量一次性执行')
s = s.replace('固定商业利润是服务器目录声明的**绝对金额**', '每座满员等效建筑的固定商业利润是服务器目录声明的**绝对金额**')
s = s.replace('商业建筑进入一级和地区统一建筑页', '商业建筑进入一级建筑页与地区商业分区')
s = s.replace('## 4. 异常、续营与自动经营', '''### 3.1 商业满员率与锁定经营量

商业满员率是独立商业经营效率，不是参与建筑数占比或周期时间。以商业集群的 `staffingRateBps`、`staffingUpdatedAt` 为权威基线；正常营业且营业意图开启时线性恢复，停止或经营异常时线性下降。恢复和下降速度与工业保持一致，正式常量及投影由 `shared/commercial-staffing.js` 负责。自动采购开关不直接改变满员率。客户端缺失基线时显示待同步，不自行补为满员；新建集群及缺少字段的旧存档由服务器在当前时刻建立满员基线，不追溯计算旧存档的停工衰减。

周期开始时投影当时满员率，计算 `(全部建筑数 × 满员率基点 + 上周期余数) ÷ 满员基点` 的整数商与余数。整数商为本周期等效经营量，商品、运营成本和基础固定利润都乘此整数商；余数仅在成功开始周期时提交并跨周期保留，不因失败、重复启停或自动采购配置改变重复累积。物理参与数量与等效经营量分别保存，计算采用安全整数／固定点，商品数量保持整数。

本周期额外锁定 `pendingStaffingRateBps` 和 `pendingEffectiveCount`，开始后实时满员率恢复、下降、价格变化和扩建均不得改写已投入商品、成本、收入或利润。同类型扩建先完成扩建时点前的旧周期，投影旧满员率，再按旧数量／新数量稀释；当前在途周期的物理参与数量与锁定结果保持不变。启停前同样按动作时点完成此前到期周期，避免把新意图追溯应用到过去。

零满员率或整数等效经营量为零时，仍可开启明确的零投入、零收入恢复周期，使用 `cycleActive` 标记并进入正式商业截止时间调度。不能用收入是否大于零代替周期是否存在，不能反复启停重置已开始的零收入周期。旧正收入在途周期可迁移出标记；缺失的旧锁定满员率／等效经营量保持未知，不用新基线回填旧结算。

## 4. 异常、续营与自动经营''')
p.write_text(s)

p = Path('docs/AUTHORITATIVE_COUNTDOWN_DESIGN.md')
s = p.read_text()
s = s.replace('HTTP 2xx、3xx 或除 408、429 之外的明确 4xx 响应表示本次网络结果已经确定，可以释放待确认 key；', '普通经济写请求的完整响应正文必须在同一次尝试时限内读取并解码。只有收到完整可解析的回执（商品动作还须包含明确的 `result.ok`、消息和修订号），且为成功状态或除 408、429 之外的明确 4xx，才允许释放待确认 key；仅收到响应头或收到损坏／缺失的动作回执不能证明结果。')
s = s.replace('确认成功后把真实响应交回原调用链', '确认成功后把已完整读取的真实响应交回原调用链')
s = s.replace('待确认表最多保存 128 项，条目最长保留 24 小时', '待确认表最多保存 128 项，达到上限时拒绝新的写操作而不驱逐尚未确认的键；条目最长保留 24 小时')
s += '''
普通游戏 API 写请求只由统一写协调层维护完整回执时限，调用层不叠加第二个普通写超时；读取继续保留独立读取时限。相同逻辑操作在同一页面内同时发起时共享一个正在执行的确认任务，各调用方获得可独立读取的回执，不并发重发同一笔操作。回执无法确认时保留原键，不把传输失败描述为已经取消；本地状态协调失败也不改写已确认的动作结果。

手动商品即时交易请求只发送实际业务意图（地区、商品、方向、数量及存档世代），不携带本地预览价或会随轮询变化的生产提案；成交价仍由服务器决定，必要生产兜底仍在既有权威事务内执行。因而刷新页面、官方价换日或生产提案变化不会为同一待确认意图换键。页面提交期间冻结本次参数，显示提交／确认状态；未确认时提供原交易的确认入口，不因资金或库存已被第一次交易更新而禁止确认。
'''
p.write_text(s)

p = Path('docs/SERVER_ARCHITECTURE_AND_DEPLOYMENT_DESIGN.md')
s = p.read_text()
s += '''
用户写入在入队时先等待当时已到期的世界调度 barrier；成功后必须直接进入同一权威串行写执行器，不能递归追赶新一轮到期调度而导致用户请求永久饥饿。barrier 失败仍阻止经济动作；调度、用户写、事务提交及幂等回执保持原串行和原子边界。沿用 `schedulerBarrierWaitMs` 和写执行阶段计时诊断等待，不能通过绕过到期经济处理或提前返回成功降低表面耗时。

商业满员率基线、余数、周期存在标记和可选锁定经营量随既有 `commercialBuildingGroups` 保存并归属 `player.production`，不把实时投影写入读请求缓存。零收入周期必须进入既有商业截止时间计算，不新增轮询或另一条结算链路。迁移、经营量和锁定语义唯一引用 `COMMERCIAL_BUILDINGS_DESIGN.md`。
'''
p.write_text(s)

p = Path('docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md')
s = p.read_text()
s = re.sub(r'不得为样式一致新增工业满员率、生产配置或[^。]*。', '商业摘要增加独立满员率进度，经济含义归 `COMMERCIAL_BUILDINGS_DESIGN.md`；仍不得带入工业生产配置或无意义的零值资产字段。', s)
s = s.replace('摘要展示总数量、本周期参与数量、营业状态、单座稳定利润与营业意图开关', '摘要展示总数量、本周期物理参与数量、营业状态、单座满员额定利润、满员率与营业意图开关')
s = s.replace('未运行时按全部建筑和本州官方价预估下一周期', '未运行时按当前满员率、整数等效经营量和本州官方价预估下一周期')
s += '''
商业满员率进度放在摘要的单座满员额定利润下方，周期进度仍留在经营结算中，二者语义不得合并。卡片与汇总中的单座利润明确为满员额定值；详情另外显示当前满员率的平均利润预估，实际经营量按整数周期锁定。经营结算分别显示物理参与数量、锁定／预计等效经营量和对应金额；旧在途周期缺少锁定满员率／等效经营量显示未知，不用实时值补齐。

商品交易操作区在请求中禁用方向、数量和重复提交，并显示正在买入／卖出或正在确认交易结果。结果未确认时保留原参数并显示“确认交易结果”，不把该按钮当作新交易，且不被已经变化的可用资金／库存限制阻挡。明确回执后恢复普通交易控件；后续状态同步异常不把已成交反馈改为失败。请求和幂等行为归 `AUTHORITATIVE_COUNTDOWN_DESIGN.md`。
'''
p.write_text(s)

p = Path('docs/PRODUCTION_PILL_ALIGNMENT_DESIGN.md')
s = p.read_text()
s += '\n工业与商业满员率轨道共用 `BuildingStaffingProgress` 的结构、填充、文字与可访问进度属性；领域适配器提供百分比、方向和解释，公共组件不计算经营量。缺失商业权威基线时显示待同步且不提供伪造的百分比。营业周期轨道继续独立放在结算面板内。\n'
p.write_text(s)
p = Path('docs/UI_DESIGN_SYSTEM.md')
s = p.read_text() + '\n`BuildingStaffingProgress` 是工业和商业满员率的纯展示组件，沿用现有工业轨道样式；商业通过自己的权威时间投影接入，不能转换成 `FacilityGroup`。字段位置归页面设计，经济效应归各领域设计。\n'
p.write_text(s)
