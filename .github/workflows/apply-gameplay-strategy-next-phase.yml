name: Apply gameplay strategy next phase

on:
  push:
    branches:
      - agent/gameplay-strategy-next-phase

permissions:
  contents: write

concurrency:
  group: gameplay-strategy-next-phase
  cancel-in-progress: true

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/gameplay-strategy-next-phase
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24.4.0
          cache: npm

      - name: Normalize and apply gameplay changes
        run: |
          python3 scripts/fix-gameplay-strategy-patch.py
          python3 scripts/apply-gameplay-strategy-next-phase.py
          git checkout HEAD -- scripts/verify-gameplay-decision-support.mjs src/utils/leaderboardGoals.ts
          python3 - <<'PY'
          from pathlib import Path

          research_path = Path('src/pages/ResearchPage.tsx')
          research = research_path.read_text(encoding='utf-8')
          conditional = '      {isSelectedActive && active ? (\n'
          section_marker = '      <section className="research-industry-context mobile-detail-section" aria-label="产业经营视角">\n'
          conditional_index = research.find(conditional)
          section_start = research.find(section_marker, conditional_index)
          if conditional_index < 0 or section_start < 0:
              raise SystemExit('Could not locate generated research industry context placement')
          section_end = research.find('      </section>\n', section_start)
          if section_end < 0:
              raise SystemExit('Could not locate research industry context end')
          section_end += len('      </section>\n')
          block = research[section_start:section_end]
          research = research[:section_start] + research[section_end:]
          conditional_index = research.find(conditional)
          research = research[:conditional_index] + block + '\n' + research[conditional_index:]
          research_path.write_text(research, encoding='utf-8')

          contract_path = Path('src/contracts/api.ts')
          contract = contract_path.read_text(encoding='utf-8')
          contract = contract.replace(
              "performance: async () => (await get<{ performance: ContractPerformanceSummary }>('/contracts/performance')).performance,",
              "performance: async () => (await getJson<{ performance: ContractPerformanceSummary }>('/contracts/performance')).performance,",
          )
          contract_path.write_text(contract, encoding='utf-8')

          market_path = Path('src/utils/marketDecisionSignals.ts')
          market = market_path.read_text(encoding='utf-8')
          market = market.replace(
              '  const latest = points.at(-1);\n  const previous = points.length >= 2 ? points.at(-2) : undefined;',
              '  const latest = points.length > 0 ? points[points.length - 1] : undefined;\n  const previous = points.length >= 2 ? points[points.length - 2] : undefined;',
          )
          market_path.write_text(market, encoding='utf-8')
          PY

      - name: Install dependencies
        run: npm ci

      - name: Verify full build
        run: npm run build

      - name: Commit verified changes and remove temporary tooling
        run: |
          rm scripts/apply-gameplay-strategy-next-phase.py
          rm scripts/fix-gameplay-strategy-patch.py
          rm .github/workflows/apply-gameplay-strategy-next-phase.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- \
            server/src/tutorial-store.js \
            server/src/player-admin-statistics.js \
            server/src/contract-audit-store.js \
            server/src/app.js \
            server/src/economic-events.js \
            server/test/tutorial-store.test.js \
            server/test/player-admin-statistics.test.js \
            server/test/contract-audit.test.js \
            server/test/economic-events.test.js \
            src/api/admin.ts \
            src/components/AdminPlayerStatistics.tsx \
            src/utils/marketDecisionSignals.ts \
            src/utils/facilityOperatingDiagnostics.ts \
            src/components/facilities/FacilityOperatingDiagnostics.tsx \
            src/styles/facility-operating-diagnostics.css \
            src/pages/production/ProductionFacilityDetail.tsx \
            src/pages/production/MobileFacilityDetailSheet.tsx \
            src/pages/ProductionPage.tsx \
            src/pages/ResearchPage.tsx \
            src/styles/research-page.css \
            src/contracts/types.ts \
            src/contracts/api.ts \
            src/pages/ContractPage.tsx \
            src/styles/contracts.css \
            src/utils/leaderboardGoals.ts \
            src/pages/LeaderboardPage.tsx \
            src/styles/leaderboards.css \
            src/pages/OverviewPage.tsx \
            src/styles/overview.css \
            scripts/verify-gameplay-decision-support.mjs \
            scripts/verify-admin-player-statistics.mjs \
            docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md \
            docs/INDUSTRY_AND_PRODUCTION_DESIGN.md \
            docs/GIFT_CODE_AND_ADMIN_DESIGN.md \
            docs/SERVER_ARCHITECTURE_AND_DEPLOYMENT_DESIGN.md \
            docs/PRODUCT_AND_GAMEPLAY_DESIGN.md \
            package.json \
            scripts/apply-gameplay-strategy-next-phase.py \
            scripts/fix-gameplay-strategy-patch.py \
            .github/workflows/apply-gameplay-strategy-next-phase.yml
          git diff --cached --check
          git commit -m "实现中期经营决策支持与完整成长漏斗"
          git push origin HEAD:agent/gameplay-strategy-next-phase
