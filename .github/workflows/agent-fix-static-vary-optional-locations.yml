name: Fix static Vary optional locations

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-static-gzip-vary-header
          fetch-depth: 0
      - name: Fix and verify optional static locations
        run: |
          set -euo pipefail
          python3 .agent/fix-static-vary-optional-locations.py
          git diff --check
          python3 scripts/test_configure_economy_nginx.py
          node scripts/verify-state-delivery-capacity.mjs
          rm -f .agent/fix-static-vary-optional-locations.py
          rm -f .github/workflows/agent-fix-static-vary-optional-locations.yml
          rmdir .agent 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "允许代理配置缺少静态位置"
          git push origin HEAD:agent/fix-static-gzip-vary-header
