name: Facility artwork payload staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Waiting for the complete facility artwork payload."
