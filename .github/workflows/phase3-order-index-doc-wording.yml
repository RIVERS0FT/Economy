name: Phase 3 order index wording

on:
  push:
    branches:
      - agent/client-response-phase3

permissions:
  contents: write

jobs:
  transform:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/client-response-phase3
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.4.0
      - run: node scripts/phase3-order-index-doc-wording.mjs
      - name: Remove temporary transformer and commit
        run: |
          rm scripts/phase3-order-index-doc-wording.mjs .github/workflows/phase3-order-index-doc-wording.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "docs: 对齐客户端订单索引术语"
          git push origin HEAD:agent/client-response-phase3
