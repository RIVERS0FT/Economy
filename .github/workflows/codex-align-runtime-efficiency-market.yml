name: Align instant market runtime efficiency authority

on:
  push:
    branches:
      - codex/daily-system-market

permissions:
  contents: write

jobs:
  align:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: node scripts/codex-align-runtime-efficiency-market.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "对齐即时市场运行时效率文档边界"
          git push origin HEAD:codex/daily-system-market
