name: Apply storage V2 deployment backup protection

on:
  push:
    branches:
      - perf/segmented-world-storage-v2

permissions:
  contents: write

jobs:
  apply:
    if: github.event.head_commit.message == 'Finalize storage V2 deployment backup verification'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/segmented-world-storage-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24.4.0'
          cache: npm
      - name: Finalize deployment backup verifiers
        run: |
          node scripts/.fix-storage-v2-backup-verifier.mjs
          git diff --check
      - name: Commit verifier cleanup
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Finalize storage V2 deployment backup verification'
          git push origin HEAD:perf/segmented-world-storage-v2
      - run: npm ci
      - name: Verify deployment storage and full build
        run: |
          node scripts/verify-deployment-storage.mjs
          npm run build
          npm run test:stress
