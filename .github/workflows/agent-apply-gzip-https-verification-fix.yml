name: Apply gzip HTTPS verification fix

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-static-gzip-https-verification
          fetch-depth: 0
      - name: Apply and verify gzip HTTPS fix
        run: |
          set -euo pipefail
          python3 .agent/apply-gzip-https-verification-fix.py
          git diff --check
          python3 scripts/test_configure_economy_nginx.py
          node scripts/verify-state-delivery-capacity.mjs
          rm -f .agent/apply-gzip-https-verification-fix.py
          rm -f .github/workflows/agent-apply-gzip-https-verification-fix.yml
          rmdir .agent 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "修复 gzip 本机 HTTPS 验收"
          git push origin HEAD:agent/fix-static-gzip-https-verification
