name: codex-map-roam-page-verifier-fix

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'codex/map-roam-performance'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/map-roam-performance
          fetch-depth: 0
      - name: Match authoritative page georoam wording
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          path = Path('scripts/verify-provincial-economy.mjs')
          source = path.read_text()
          old = "  'georoam 期间不得重新执行 48 州标签完整布局',"
          new = "  '`georoam` 期间不得重新执行 48 州标签完整布局',"
          if source.count(old) != 1:
              raise SystemExit(f'page georoam wording count={source.count(old)}')
          path.write_text(source.replace(old, new, 1))
          PY
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/codex-map-roam-page-verifier-fix.yml
          git add scripts/verify-provincial-economy.mjs
          git commit -m "修正地图性能页面设计校验字面匹配"
          git push origin HEAD:codex/map-roam-performance
