name: Fix Facility Verifier

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  fix-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/population-demand-price-transmission
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Align world version assertion
        run: |
          node - <<'NODE'
          const fs = require('node:fs');
          const path = 'scripts/verify-facility-groups.mjs';
          const source = fs.readFileSync(path, 'utf8');
          fs.writeFileSync(path, source.replace("'world.version = 9'", "'world.version = 10'"));
          NODE
      - name: Verify
        run: node scripts/verify-facility-groups.mjs
      - name: Commit verifier fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/verify-facility-groups.mjs
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "同步工厂集群验证到世界版本 10"
          git push origin HEAD:agent/population-demand-price-transmission
