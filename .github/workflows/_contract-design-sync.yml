name: Contract design sync

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: write

jobs:
  sync:
    if: github.head_ref == 'codex/contracts-core-workspace'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Update authoritative contract design
        run: |
          python - <<'PY'
          from pathlib import Path

          product_path = Path('docs/PRODUCT_AND_GAMEPLAY_DESIGN.md')
          product = product_path.read_text()
          old_experience = '> 判断供需与价格，组织产业链，控制工厂生产，配置资产，并持续提升自己的总资产与排行榜名次。'
          new_experience = '> 判断供需与价格，围绕长期合同组织产业链，在现货与合同之间配置经营风险，控制生产、库存与资金，并持续提升自己的总资产与排行榜名次。'
          assert old_experience in product
          product = product.replace(old_experience, new_experience, 1)

          old_loop = '''```text
          获得首次建档启动资金
          → 建设或买入工厂
          → 选择生产配方并开启工厂集群
          → 消耗资金和原料完成完整周期
          → 产成品进入工厂所在州级地区的本地无限仓库
          → 在同一州级地区的统一订单簿买卖商品，工厂所有权仅通过拍卖转移
          → 消费需求形成最终品订单并沿正式配方派生上游流动性，市场储备提供双边流动性
          → 调整产业链、库存和现金
          → 提升资产并参与排行榜
          ```'''
          new_loop = '''```text
          获得首次建档启动资金
          → 观察州级现货供需、公开合同与自身生产缺口
          → 在即时现货与长期合同之间选择采购、供货、融资或租赁关系
          → 发布、承接或议价合同，并围绕已建立的长期关系建设／租入工厂与组织生产
          → 消耗资金和原料完成完整周期，产成品进入工厂所在州级地区的本地无限仓库
          → 合同覆盖的需求按权威条款准备、托管、履约和结算，未覆盖的缺口与剩余产出继续进入同州统一订单簿
          → 跨州缺口只通过正式运输调整，工厂所有权只通过拍卖转移
          → 根据合同履约、现货价格、库存和现金重新调整产业链与资产配置
          → 提升资产并参与排行榜
          ```'''
          assert old_loop in product
          product = product.replace(old_loop, new_loop, 1)

          old_growth = '生产、产业链和交易是玩家持续增长来源；不存在可重复点击领取普通货币的基础工作玩法。'
          new_growth = '''合同是连接生产、市场、库存、运输与玩家资本关系的核心长期经营机制。现货市场负责即时价格发现与即时成交，合同负责锁定未来经营关系；两者共同组成经营循环，合同不得创建第二套订单簿、跨州库存或绕过正式运输。

          玩家通过商品供货／采购、玩家抵押借贷和工厂使用权租赁三类合同建立稳定合作，再用现货市场处理没有被合同覆盖的即时缺口与剩余产出。工厂生产继续按现有权威采购规则比较同州合同固定价与真实市场可执行价格，不因“合同核心化”改成无条件优先合同，也不允许客户端替服务器决定实际使用来源。

          当前核心化只重排经营入口、决策顺序和长期合作可见性，不新增最低承诺量、运输委托、代工合同、主观信用分或合同专属货币。上述新业务模型只有在对应业务与服务器权威设计完成后才能进入正式规则。

          生产、产业链、合同和交易共同构成玩家持续增长来源；不存在可重复点击领取普通货币的基础工作玩法。'''
          assert old_growth in product
          product = product.replace(old_growth, new_growth, 1)
          product = product.replace('合同广场', '合同市场')
          product_path.write_text(product)

          page_path = Path('docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md')
          page = page_path.read_text()
          start = page.index('## 7. 合同\n')
          end = page.index('## 8. 银行\n', start)
          new_contract_section = '''## 7. 合同

          合同页是玩家长期经营关系的工作中心，不是一次性交换列表。页面主标题固定为“合同”，使用路由 ID `contracts` 和 `ContractPage`；标题下不得显示灰色说明副标题，标题区也不承载发布操作。“发布合同／收起发布表单”按钮固定放在正文第一行并右对齐，之后依次为四项摘要、可选发布面板、合同工作区。

          合同一级工作区固定使用四项共享分段按钮：**工作台｜合同市场｜我的合同｜历史**。普通进入合同页默认进入“工作台”；只有来自既有商品或工厂经营上下文的合法 `provinceId + productId` 合同导航意图可以直接进入“合同市场”并预选地区与商品。工作区切换只改变当前合同视图，不创建新路由、第二套合同状态或独立滚动容器，也不得恢复合同广场与我的合同桌面常驻双栏。

          合同页顶部四项摘要固定为“等待我处理／24 小时内履约／进行中的合同／我的公开合同”。四项属于同一比较条，在宽布局四列同排，宽度不大于 `960px` 时固定为两列，移动端继续保持两列。等待处理包含服务器合同异常、旧兼容宽限／已违约待解除、当前日结束申请和需要本人回应的结构化议价；24 小时内履约只读取服务器权威下一状态边界，不由浏览器倒计时推断。

          ### 7.1 工作台

          “工作台”回答“现在最需要处理哪份合同”。存在待处理合同时左侧主列表只优先排列待处理合同；没有待处理合同时按下一状态边界排列全部进行中合同。桌面采用左侧选择、右侧完整详情的主从结构：左侧只显示合同身份、我的方向、关键价格或状态和待处理标记，不把完整合同卡全部铺开；右侧只渲染当前选中合同的一张完整对象卡并保留全部履约操作。内容宽度不大于 `960px` 时主从结构降为单列，列表位于详情之前；页面始终只有统一正文纵向滚动。

          工作台下方固定展示“我的履约档案”，只从服务器合同追加式审计汇总已结束合同总数、正常完成数／完成率、异常结束数、违约或主动违约次数、当前玩家方向的累计赔付／获赔和最近五份合同结果。履约档案不得生成星级、S/A/B 信用等级、黑名单分或其他主观评分，也不得暴露非参与者身份。

          ### 7.2 合同市场

          合同市场按领域、合作方向、地区和商品筛选。领域覆盖商品合作、玩家借贷和工厂租赁；“合作方向”只在商品合作中表达玩家意图：“我要采购”筛选供应方公开报价，“我要供货”筛选采购方公开需求。合作方向是合同页内的显式筛选，不得由工厂投入／产出、市场买卖方向或其他页面自动推断。

          建筑页工厂经营诊断及既有商品合同简要中的“查看相关合同”仍只携带当前 `provinceId + productId` 并打开合同市场，不自动判断采购／供应方向、数量、价格、周期或发布类型。该入口只负责缩小候选范围，玩家必须在合同市场自行选择合作方向和具体合同。

          合同市场在桌面同样使用左侧选择、右侧完整详情：左侧是扁平公开合同列表，右侧只显示当前选中的一份完整公开合同对象卡并提供议价、承接或发布者取消操作。筛选器直接排列在页面分区中，不套大圆角外壳；宽度不大于 `960px` 时列表与详情上下排列。公开合同对象卡继续保持独立业务对象边界，不得为了主从布局把对象卡完全扁平化。

          市场储备采购继续作为合同市场中的固定条款采购对象显示，明确标记“市场储备采购”，不显示议价或续签入口；不得为市场储备增加第二套合同页、导航或轮询。

          ### 7.3 我的合同

          “我的合同”只展示当前进行中的本人合同，按“已确认违约待解除 → 其他待处理 → 下一状态边界 → 正常履约”排序。桌面继续使用左侧选择、右侧完整详情；列表不复制完整条款，右侧对象卡承载当前额度、托管、自动准备／补款、立即准备／补款、结束申请、违约处置、借贷还款和租赁补款等既有操作。不得恢复同时铺开多张完整进行中合同卡，也不得把待处理状态只用颜色表达。

          进行中每日额度商品合同卡先展示当日额度状态，再展示固定条款。今日已使用、每日上限、今日剩余额度、当日货款托管、当日商品预留、固定价格、地区、累计交付和下一状态边界属于当前履约事实。每日额度合同没有“必须交满每日额度”的违约条件，未使用额度只在北京时间跨日时释放当日货款和商品预留并重置。供应方自动准备与采购方自动补款继续使用 `ToggleField`／`SwitchControl`；手动准备／补款只处理当日剩余额度。供应方可设置“最低当日产量 + 最低合同价格”两个优先供应条件，两个条件同时满足后新增可用库存才自动预留；手动准备不受该自动条件阻断。

          每日额度商品合同不显示续签区；长期合同可以申请按当前北京时间自然日结束，有限合同按期限结束，立即终止继续按发起方承担权威保证金责任。旧有限批次商品合同的当前批次、续签、宽限与受偿方主动解除界面只保留兼容展示；旧兼容续签中提出续签条款不代表同意续签，采购方与供应方仍必须分别明确同意，单方同意不冻结资产，不得被复制回新每日额度合同。

          ### 7.4 历史与履约事实

          “历史”是独立全宽工作视图，不与合同市场或我的合同常驻并排。历史使用单张一级 `PagePanel` 和带分隔线的紧凑记录；筛选器覆盖合同领域、最终状态、我的角色、合同标的和日期范围。我的角色覆盖采购、供货、放贷、贷款、出租、租赁和原发布者；合同标的覆盖商品、普通货币和工厂类型。

          每条历史合同固定展示合同内容、结束原因、结束时间、完成事实、结束统计和“重新拟定”按钮。每日额度商品合同的完成事实以累计实际交付数量、实际交付事件和合同持续天数为准，不伪造“必须完成的每日数量”或批次完成率；旧有限批次／旧长期商品合同继续按历史批次语义展示，借贷按一次性偿还、租赁按租金期数展示。结束原因和时间来自服务器终态摘要，客户端不得根据余额或本地时间推断；旧两阶段违约合同继续区分 `breachedAt` 与最终 `endedAt`。结束统计按当前玩家视角展示真实货款／租金、服务费、净收入、实际偿还、抵押处置、资金或商品退回以及非零赔付；旧合同缺少完整转移事件时显示部分统计提示，不伪造缺失过程。

          玩家历史页不展开、不加载审计事件时间线，也不把整条记录做成无明确目标的可点击表面。服务器继续保留追加式事件和资产转移审计及参与者审计接口，用于故障追溯、管理员诊断和未来纠纷核查。点击“重新拟定”后展开页面顶部既有发布合同 `PagePanel`，按当前玩家在原合同中的实际角色恢复六类发布方向并填入相同合同参数；不复制合作方、托管资产、保证金余额、自动操作状态、完成数量、续签关系或结束信息，也不得自动发布。若发布面板已有未发布内容，替换前必须确认。

          ### 7.5 发布、议价与三类合同领域

          合同只允许商品合作、玩家抵押借贷和工厂使用权租赁三个正式领域。商品合作只使用服务器正式商品和普通货币；玩家借贷只使用普通货币本金、固定总利率和数量化工厂抵押；工厂租赁只转移 `facilityTypeId + quantity` 的临时生产使用权。不得加入工厂所有权转移、单座工厂实例、指定配方、对方工厂启停控制、自由文本或三类领域之外的其他资产。发布面板固定先展示供应合同、采购合同、放贷合同、贷款合同、出租合同和租赁合同六种入口，再显示所属领域条款与风险预览；合同标题由服务器权威条款生成。

          发布合同面板必须使用 `PagePanel`。宽度不小于 `1220px` 时采用“左侧合同条款／右侧合同预览”的双列工作台，`721px–1219px` 时改为单列面板并让条款字段保持两列，宽度不大于 `720px` 时字段全部单列。商品采购／供应使用 `SelectInput` 选择合同地区与商品，使用 `IntegerInput`／`MoneyInput` 编辑每日最大供应量、固定价格、合同时间和开始延迟；合同时间留空提交 `durationDays = null` 表示长期，不得恢复总批次、交付周期或首次交付分钟／小时输入。借贷期限与租赁周期、首次生效也统一以天显示和提交；服务器内部兼容毫秒字段不得暴露为新表单单位。

          玩家新发布的商品采购／供应合同统一使用地区化每日额度模型，必须指定合同地区、商品、每日最大供应量、固定价格、合同时间（天，可留空表示长期）和开始延迟（天）。每日最大供应量只表示北京时间自然日内最多可实际使用的数量，未使用额度不补算、不跨日累计。直接承接始终保留并可选结构化议价；议价只能修改每日最大供应量、固定价格、合同时间和开始延迟，商品、地区、发布方向和发布者固定。每份公开商品合同最多同时存在 3 个议价线程、每名玩家最多一个线程、每线程最多 5 轮，每次报价 24 小时无回应即失效且不得超过公开合同有效期。议价阶段不冻结资产；任一方接受对方最后报价时，服务器重新校验地区权限、参与数量、日额度货款和双方保证金后才原子进入履约。商品议价只向发布者和对应发起者展示，第三方不得看到议价身份、报价、线程数量或内部玩家 ID。

          新每日额度商品合同不使用续签：有限合同到期后可重新发布，长期合同可申请当前北京时间自然日结束后正常终止。市场储备采购和已存在的旧商品合同继续按原有限批次／旧长期语义运行，只作为兼容路径，不得重新成为新发布入口；旧玩家商品合同协议中的 `totalDeliveries = null` 继续表示旧长期合同，旧长期合同不会因完成批次数自动结束且不接受续签。

          ### 7.6 权威履约与违约边界

          合同承接、托管、准备、补款、交付、还款、租赁结算、宽限、违约确认、赔付和结束都由服务器权威执行。商品每日额度合同使用当日货款托管、供应方商品预留和双方履约保证金；自动操作只能冻结当前可用库存或当前可用资金，不能透支未来产量或未来收入。合同不绑定或拆分同类型工厂集群，不修改运行意图、当前配方或生产周期。

          宽限结束只确认违约，不得直接解除玩家合同。服务器记录责任方与 `breachedAt` 后停止后续履约；与违约赔偿无关的托管资金、商品和无责方保证金按权威规则释放，责任方保证金继续冻结作为待领取赔偿。违约确认后不得通过事后补货、补款、还款或重新开启自动履约恢复合同。受偿玩家必须主动执行对应解除／领取动作；责任方不能自行解除来逃避赔付。双方均违约时按服务器权威规则解除且不发生单方赔付。玩家抵押借贷的抵押处置和工厂租赁违约后的使用权停止同样由服务器执行；市场储备作为无玩家操作主体时可以按服务器规则自动处置，例外不得扩大到玩家受偿合同。

          合同交付不写入统一订单簿最近成交价、价格曲线、商品估值或交易榜。客户端倒计时归零后只能等待服务器状态确认，不得自行宣布交付、完成或违约。合同历史通过 `GET /api/game/contracts/history` 按需加载终态摘要，只允许参与者查看；普通玩家合同页不得请求 `GET /api/game/contracts/:contractId/audit` 或展示事件时间线。

          ### 7.7 布局与防回退边界

          四项工作区按钮必须复用 `Button` 与 `.ui-segmented`，提供 `tablist`、`tab`、`tabpanel` 和 `aria-selected` 语义；桌面四项同排，宽度不大于 `720px` 时固定为两列两行，`320px` 与 `390px` 宽度下不得产生页面横向溢出。工作台、合同市场和我的合同均使用同一主从结构，桌面左侧选择、右侧完整详情，宽度不大于 `960px` 时降为单列；不得给左右区域增加独立滚动条。

          公开合同和进行中合同逐份保留独立对象卡边界，但同一工作视图只渲染当前选中对象的一张完整卡；主列表使用分隔线和选择状态，不创建每行圆角卡。发布表单、筛选器、履约档案、历史集合和工作区分区属于页面结构，不得为了视觉分组增加大圆角外壳。不得恢复合同广场与我的合同桌面常驻双栏，不得恢复“进行中的合同／历史合同”只切右半屏的旧结构，也不得把多张完整合同卡重新同时铺成卡片墙。

          正文发布操作行、合同摘要网格、可选发布面板、四项共享分段按钮和当前工作视图必须按该顺序作为 `PageLayout` 自动生成的 `.ui-page-stack` 直接子元素或该顺序中的当前视图内容；一级区块间距只由共享 `.ui-page-stack` 管理，合同业务样式不得通过页面外边距抵消统一间距。

          ## 8. 银行
          '''
          page = page[:start] + new_contract_section + page[end + len('## 8. 银行\n'):]
          page = page.replace('合同广场中的固定条款采购卡片', '合同市场中的固定条款采购对象')
          page = page.replace('让合同页面默认进入合同广场而隐藏进行中的合同', '让合同页面默认进入合同市场、我的合同或历史而跳过工作台，或恢复合同市场与我的合同桌面常驻双栏')
          page = page.replace('把宽屏合同广场恢复为单列、强制同行卡片等高，或把合同历史恢复为每条一张独立一级玻璃卡片', '把合同市场或我的合同恢复为多张完整合同卡同时铺开、强制同行卡片等高，或把合同历史恢复为每条一张独立一级玻璃卡片')
          page = page.replace('合同广场', '合同市场')
          page_path.write_text(page)

          ui_path = Path('docs/UI_DESIGN_SYSTEM.md')
          ui = ui_path.read_text()
          old_ui = '合同页作为当前对象卡样板：合同广场与“我的合同”是页面分区，不使用外层圆角；筛选器直接排列；公开合同和进行中合同逐份使用独立对象卡；发布合同、历史筛选和履约档案仍属于页面结构；合同顶部四项摘要使用无逐项圆角的同一摘要条。对象卡内部的条款、进度、自动化、续签和操作可以使用细线或 `--radius-control` 的局部 inset block，但不得再套第二层 `--radius-card` 大卡片或毛玻璃。'
          new_ui = '合同页作为当前对象卡样板：工作台、合同市场、“我的合同”和历史是页面分区，不使用外层圆角；工作台、合同市场和“我的合同”的主列表保持扁平，当前选中的公开合同或进行中合同使用独立对象卡；筛选器直接排列；发布合同、历史筛选和履约档案仍属于页面结构；合同顶部四项摘要使用无逐项圆角的同一摘要条。对象卡内部的条款、进度、自动化、续签和操作可以使用细线或 `--radius-control` 的局部 inset block，但不得再套第二层 `--radius-card` 大卡片或毛玻璃。'
          assert old_ui in ui
          ui_path.write_text(ui.replace(old_ui, new_ui, 1))

          surface_path = Path('docs/PRIMARY_SURFACE_INSET_DESIGN.md')
          surface = surface_path.read_text()
          old_surface = '页面章节、筛选区、发布表单大分组、资产总览、合同广场、“我的合同”、履约档案等集合或页面结构不得仅为了视觉分组增加圆角外壳。'
          new_surface = '页面章节、筛选区、发布表单大分组、资产总览、合同工作台、合同市场、“我的合同”、历史筛选、履约档案等集合或页面结构不得仅为了视觉分组增加圆角外壳。'
          assert old_surface in surface
          surface_path.write_text(surface.replace(old_surface, new_surface, 1))

          industry_path = Path('docs/INDUSTRY_AND_PRODUCTION_DESIGN.md')
          industry = industry_path.read_text()
          industry_path.write_text(industry.replace('合同广场', '合同市场'))
          PY
          git diff --check
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/PRODUCT_AND_GAMEPLAY_DESIGN.md docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md docs/UI_DESIGN_SYSTEM.md docs/PRIMARY_SURFACE_INSET_DESIGN.md docs/INDUSTRY_AND_PRODUCTION_DESIGN.md
          git commit -m "将合同提升为核心经营工作区"
          git push origin HEAD:${{ github.head_ref }}
