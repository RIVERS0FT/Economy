# Economy

Economy 是一款多人在线经济模拟与交易游戏。

- 项目：`RIVERS0FT/Economy`
- 生产网页：`https://game.riversoft.top/economy/`
- 当前方向：网页端多人联机金融与产业经营
- 客户端状态版本：8
- 世界状态版本：4

页面内容与模块归属以 `docs/PAGE_CONTENT_AND_NAVIGATION_DESIGN.md` 为准；工厂集群和市场第三版以 `docs/FACILITY_GROUP_AND_MARKET_V3_DESIGN.md` 为准。

## 核心循环

```text
工作获得基础资金
→ 扩容共享仓库
→ 建设同类型工厂数量
→ 统一设置生产计划并启动集群
→ 产成品直接进入共享仓库
→ 使用快捷仓位提交商品订单
→ 在单列订单簿观察买卖盘
→ 按数量挂牌或购买工厂
→ 提升总资产与排名
```

## 工厂类型集群

工厂不再以单座实例保存。玩家资产按类型和数量表示：

```text
农场 × 8
矿场 × 4
面粉厂 × 3
```

每种工厂类型只有一个统一状态、一条统一进度、一个统一生产计划和一组统一启停操作。

不存在工厂实例 ID、实例列表、展开实例、单座启停、工厂内部仓库或领取产成品。

### 统一周期

```text
周期产量 = 单座周期产量 × 当前参与数量
周期成本 = 单座周期成本 × 当前参与数量
周期原料 = 单座原料 × 当前参与数量
```

生产完成后产成品直接进入共享仓库。

### 新数量下一周期加入

施工完成或市场收购发生在集群运行期间时：

```text
当前参与数量保持不变
新增数量进入 pendingJoinCount
当前周期按旧数量完成
下一周期开始时加入统一生产
```

新增数量不会重置当前进度，也不会获得部分周期产出。

同类型工厂只能全部启动或全部停止。持续和定量计划作用于整个集群。定量目标必须是集群周期产量的整数倍；数量变化后剩余目标不兼容时暂停并要求重新确认。

## 共享仓库

所有商品共用一个仓库。

```text
实物库存 = Σ(可用商品 + 冻结商品)
买单预占 = Σ(当前玩家未完成买单剩余数量)
仓库使用 = 实物库存 + 买单预占
```

扩容规则：初始 1 级、500 容量；每级增加 250；最高 12 级、3250 容量；费用为 `150 × 当前等级²`。

共享仓库完整管理卡固定在“生产”页面，其他页面不提供扩容。

## 商品市场第三版

下单顺序固定为：

```text
方向
限价
数量
1/4 仓｜1/2 仓｜全仓
订单总额
提交订单
```

买入快捷数量按 `min(仓库剩余容量, floor(可用资金 / 限价))` 计算。卖出快捷数量按当前商品可用库存计算，冻结商品不能卖出。

订单簿固定为单列：卖盘最优 5 笔、最近成交与价差、买盘最优 5 笔。每行只显示方向、价格和剩余数量，不显示玩家身份，不做价格档位聚合。

订单管理已合入市场，玩家可以在当前页直接撤单。

## 工厂数量市场

工厂挂牌按“工厂类型 + 数量 + 单座价格”保存。玩家可以一次挂牌多座，也可以购买挂牌中的部分数量。

服务器在一个事务中更新买卖双方资金、双方工厂数量、挂牌剩余数量，以及买方运行中集群的下一周期待加入数量。

存在挂牌数量时，该类型集群不能启动。

## 页面结构

```text
概览：工作、行情、工厂集群摘要、财富、本地成交
市场：快捷仓位下单、单列 5+5 订单簿、撤单、工厂数量市场
生产：共享仓库、建设任务、每种工厂一张统一集群卡
资金：资金、商品和工厂数量资产、本地变化
排行：服务器总资产排行榜
设置：玩家资料、客户端偏好、登录会话、重置
```

正式导航固定为：

```text
概览｜市场｜生产｜资金｜排行｜设置
```

## 服务器权威边界

服务器保存资金、商品库存、仓库、工厂类型集群、统一周期、统一计划、施工任务、商品订单、工厂数量挂牌、市场、统计、排行榜和幂等响应。

SQLite 世界状态不保存：

```text
player.facilities
factory instance id
internalGoods
internalCapacity
trades
ledger
assetEvents
```

## 本地活动日志

日志使用：

```text
economy.local-activity.v2.<userId>
```

日志按账号保存在当前浏览器，不上传服务器，不参与资产、仓库、工厂数量、生产、订单或排名计算。

## 数据与部署

- 游戏 API：`127.0.0.1:3002`
- 账号服务：`127.0.0.1:3001`
- 数据库：`/var/lib/riversoft-economy/economy.sqlite`
- systemd：`riversoft-economy-api.service`
- 网页目录：`/var/www/game/economy`
- API 目录：`/var/www/game/economy-api`

## 本地开发与完整检查

```bash
npm install
npm run dev
npm run build
```

完整构建覆盖页面职责、统一 UI、工厂集群、下一周期加入、数量工厂市场、快捷仓位、单列订单簿、仓库扩容、本地日志、Nginx、服务器测试、TypeScript 和 Vite 生产构建。
