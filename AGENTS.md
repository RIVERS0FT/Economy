# Economy 仓库协作规则

## 项目标识

- “项目”指 `RIVERS0FT/Economy`。
- “游戏网页”指 `https://game.riversoft.top/economy/`，“管理员页面”指 `https://game.riversoft.top/economy/admin`。
- “主页”指 `https://riversoft.top/`，“主页项目”指 `RIVERS0FT/RIVERSOFT_WEBSITE`。

## 开始任务

1. 先检查当前工作区、分支和远端，保留用户已有修改，不覆盖无关内容。
2. 先阅读 `docs/README.md`，再按任务范围阅读对应权威设计文档、当前实现、测试和防回退脚本；不要求加载与任务无关的全部文档和代码。
3. 纯分析、方案或审查请求只做读取和报告；用户要求实现时才修改、提交和部署。

## 文档层级与权威来源

- `AGENTS.md` 只规定协作流程，不保存项目介绍、业务参数、页面细节、运行时版本或部署数值。
- 根 `README.md` 只负责公开项目入口、稳定的高层能力、开发启动、常用命令、目录导航和文档入口；不得成为业务规则或内部架构规范的副本。
- `docs/README.md` 只负责设计文档索引、README 层级边界、各 DESIGN 的唯一职责／明确不负责范围和规则路由；不得保存实际业务口径、算法、UI 几何或实现路径清单。
- 目录级 `README.md` 只有在该目录确实需要工程导航时才新增，只描述本目录职责、入口和开发操作，不得定义跨目录架构或产品规则。
- 业务规则、交互规则、架构约束和部署规则只写入 `docs/README.md` 登记的对应权威 DESIGN；运行时常量以实现代码或正式数据文件为准，并由测试和 `scripts/verify-*.mjs` 防回退。
- 一个语义规则只能有一个 DESIGN owner；其他文档只说明本领域影响并引用 owner，不复制完整规则。

## 权威冲突与设计记录

- 修改规则时直接更新现有权威文档和对应验证，不得新建“补充说明”、V2/V3 或平行专题文档，也不得把详细规则复制回 README。
- 设计文档只保留当前最终规则、职责边界和必要的 `non-obvious reason`；不得保留 `intermediate attempts`、方案演进、失败尝试、临时状态或已被替代内容。
- 设计文档、实现、测试或验证脚本互相冲突时，必须在回复开头提醒用户；先核对当前代码与测试，不得静默选择或把旧规则写回。

## 修改与验证

- 修改应遵守任务对应的权威设计；新增或改变规则时，在同一变更中更新权威文档和防回退检查，避免后续修改回来。
- 涉及页面标题的修改时，除非对应权威设计明确要求副标题，默认不得在主标题下新增灰色小标题；需要例外时先更新对应权威设计文档并写明必要的 `non-obvious reason`。
- 使用精确依赖版本和 `package-lock.json`；安装依赖使用 `npm ci`，不要通过无关升级扩大变更范围。
- 小范围改动默认按 `scripts/select-ci-tests.mjs` 生成的同一计划分域执行 DT、IT 与需要的 ST；仅在计划为 `full`、涉及部署链路或共享基础设施、或排查测试波动时运行完整 `npm run build` 与 `npm run test:browser`。
- 提交前检查 `git diff --check`、完整差异和工作区状态，确保没有秘密、生成垃圾、调试输出或无关文件。
- 测试报告、截图、trace、覆盖率及其他可再生成产物不得提交到 `main`；手写测试、fixture 和有意维护的测试页面除外。

## 集成与交付

- PR 描述只记录合入后的最终行为；不得写 diff 看不出来的取舍、`intermediate attempts`、临时开发过程或任何从未合入的状态。
- 实施型任务在验证通过后，将同一目的的修改压缩为清晰提交并合入 `main`；`main` 上的 `.github/workflows/deploy.yml` 负责正式部署和线上验收。
- 若合并、部署或线上验收失败，继续定位并修复；不得把本地修改或动作已经提交后的补拉失败误报为完整成功。
- 最终报告应列出修改内容、验证结果、提交或合并状态、部署结果，以及任何尚未完成的风险或阻塞。
