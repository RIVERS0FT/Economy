# Economy 仓库协作规则

## 项目标识

- “项目”指 `RIVERS0FT/Economy`。
- “游戏网页”指 `https://game.riversoft.top/economy/`。
- “管理员页面”指 `https://game.riversoft.top/economy/admin`。
- “主页”指 `https://riversoft.top/`，“主页项目”指 `RIVERS0FT/RIVERSOFT_WEBSITE`。

## 开始任务

1. 先检查当前工作区、分支和远端，保留用户已有修改，不覆盖无关内容。
2. 先阅读 `docs/README.md`，再按任务范围阅读对应权威设计文档、当前实现、测试和防回退脚本；不要求加载与任务无关的全部文档和代码。
3. 纯分析、方案或审查请求只做读取和报告；用户要求实现时才修改、提交和部署。

## 权威来源与冲突

- `AGENTS.md` 只规定协作流程，不保存会随产品迭代变化的业务参数、版本号、页面细节、服务器路径或部署数值。
- 业务规则及唯一职责以 `docs/README.md` 登记的权威文档为准；运行时常量以实现代码为准，并由测试和 `scripts/verify-*.mjs` 防回退。
- 修改规则时直接更新现有权威文档和对应验证，不得新建“补充说明”、V2/V3 或平行专题文档，也不得把详细规则复制回本文件。
- 设计文档只保留当前最终规则和必要的 `non-obvious reason`；不得保留 `intermediate attempts`、方案演进、失败尝试、临时状态或已被替代内容。
- 设计文档、实现、测试或验证脚本互相冲突时，必须在回复开头提醒用户；先核对当前代码与测试，不得静默选择或把旧规则写回。

## 修改与验证

- 修改应遵守任务对应的权威设计；新增或改变规则时，在同一变更中更新权威文档和防回退检查，避免后续修改回来。
- 涉及页面标题的修改时，除非对应权威设计明确要求副标题，默认不得在主标题下新增灰色小标题；需要例外时先更新对应权威设计文档并写明必要的 `non-obvious reason`。
- 使用精确依赖版本和 `package-lock.json`；安装依赖使用 `npm ci`，不要通过无关升级扩大变更范围。
- 小范围改动默认本地分域验证：先读取 `scripts/select-ci-tests.mjs plan` 依据本次改动文件生成的计划，再执行 `run --phase checks` 与计划包含的 `run --phase browser`；该计划对前端改动已包含 `typecheck` 与 `vite build`。仅当计划为 `full`、改动涉及部署链路或共享基础设施、或正在排查测试波动时，才在本地运行完整 `npm run build` 与 `npm run test:browser`；完整浏览器回归门禁由 CI 的 PR 检查与 `deploy.yml` 分片执行，本地分域验证通过即可按交付流程推进。
- 提交前检查 `git diff --check`、完整差异和工作区状态，确保没有秘密、生成垃圾、调试输出或无关文件。
- 测试运行结果、报告、截图、trace、覆盖率及其他可再生成产物只保留在本地或 CI artifacts，不得提交到 `main`；稳定输出路径必须加入 `.gitignore`，已被跟踪的生成产物应在同一变更中移出版本控制。手写测试用例、fixture 和有意维护的测试页面不属于此类生成产物。

## 集成与交付

- PR 描述只记录合入后的最终行为；不得写 diff 看不出来的取舍、`intermediate attempts`、临时开发过程或任何从未合入的状态。
- 实施型任务在验证通过后，将同一目的的修改压缩为清晰提交并合入 `main`；`main` 上的 `.github/workflows/deploy.yml` 负责正式部署和线上验收。
- 若合并、部署或线上验收失败，继续定位并修复；不得把本地修改或动作已经提交后的补拉失败误报为完整成功。
- 最终报告应列出修改内容、验证结果、提交或合并状态、部署结果，以及任何尚未完成的风险或阻塞。
